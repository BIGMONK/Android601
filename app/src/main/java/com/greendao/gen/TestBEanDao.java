package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import youtu.android601.db.TestBEan;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TEST_BEAN".
*/
public class TestBEanDao extends AbstractDao<TestBEan, Long> {

    public static final String TABLENAME = "TEST_BEAN";

    /**
     * Properties of entity TestBEan.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property BranchId = new Property(2, String.class, "branchId", false, "BRANCH_ID");
        public final static Property Duration = new Property(3, long.class, "duration", false, "DURATION");
        public final static Property Position = new Property(4, long.class, "position", false, "POSITION");
    };


    public TestBEanDao(DaoConfig config) {
        super(config);
    }
    
    public TestBEanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TEST_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"BRANCH_ID\" TEXT NOT NULL ," + // 2: branchId
                "\"DURATION\" INTEGER NOT NULL ," + // 3: duration
                "\"POSITION\" INTEGER NOT NULL );"); // 4: position
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TEST_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TestBEan entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
        stmt.bindString(3, entity.getBranchId());
        stmt.bindLong(4, entity.getDuration());
        stmt.bindLong(5, entity.getPosition());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TestBEan entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
        stmt.bindString(3, entity.getBranchId());
        stmt.bindLong(4, entity.getDuration());
        stmt.bindLong(5, entity.getPosition());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public TestBEan readEntity(Cursor cursor, int offset) {
        TestBEan entity = new TestBEan( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.getString(offset + 2), // branchId
            cursor.getLong(offset + 3), // duration
            cursor.getLong(offset + 4) // position
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TestBEan entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setBranchId(cursor.getString(offset + 2));
        entity.setDuration(cursor.getLong(offset + 3));
        entity.setPosition(cursor.getLong(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(TestBEan entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(TestBEan entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
